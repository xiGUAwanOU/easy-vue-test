export default {
  /**
   * @deprecated Debugging only. Don't leave calls to this function in the code.
   */
  logHtmlCode() {
    // eslint-disable-next-line no-console
    console.log(this.getRootElement())
  }
}
